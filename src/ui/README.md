# UI

A set of components and utilities for building user interfaces with React.

You can boot up a minimal dev server with `npm run ui` to view the demos and experiments with various components.

## Architecture

These components are meant to be self-contained and make minimal assumptions about the rest of the application.