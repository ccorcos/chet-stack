# Database

This is where we handle all the schemas, query helpers, and migrations for the database.

We're currently using `src/tupledb` on to of Sqlite but could consider lmdb or eventually Postgres.
